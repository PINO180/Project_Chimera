✅ KEEP(これから常用する)

remeasure_skew_exact.py — 唯一正しい物差し(exact-timestamp)。今後の skew 計測はこれ一本。
emit_train_proxy.py — train_proxy(X 突合の ground truth)生成器。remeasure_skew_exact の X 測定に必要なので残す。

(※ realtime_feature_engine.py は models ではなく /workspace/execution/ の本番コード。別管理。)
📦 ARCHIVE(skew とは別用途で再利用価値あり。退避して保管)

inspect_live_buffer.py — state の data_buffers を S1 と突合。入力バッファの健全性チェック用(data_buffers はまだ ts 無しなので、入力側を見る唯一の手段)。
inspect_qastate_seed.py — QAState の 5σ クリップ境界inspector。QA/クリップ調査という別軸で使える。
resample_gap_compare.py — pandas/polars リサンプル一致の検証。リサンプル疑義が出た時の再利用可。
probe_buffer_truncation.py — stable_rolling の文脈独立性チェック。特徴量式を疑う時の再利用可。

🗑 DELETE(アーティファクト追跡用・役目終了・superseded)
すべて positional/cutoff 依存で、remeasure_skew_exact に置換済み:

verify_dump_deque_order_v2.py ← 7565 アーティファクトの発生源
probe_y_singlepoint.py
inspect_worst_bar.py
probe_gap_bar_mechanism.py
verify_y_exact_alignment.py(off-by-one を証明した。役目完了)
remeasure_skew_clean.py(cutoff 版。79%/7% の幻を出した)
verify_proxy_exact.py(global vs per-TF の誤突合)
remeasure_from_state.py(positional buffer 版)
diagnose_m05.py
characterize_m05_drift.py
find_m05_offset.py
verify_m05_dedup_exact.py


12 個を「今後の仮説 I-IV 検証で使うか」 で 3 分類しました。
【いる】 今後の検証で能動的に使う中核 (6 個)
スクリプト	役割	今後の用途
verify_dump_deque_order.py	dump x_deque/y_deque vs 学習側の X/Y 比較	仮説 I (位相ズレ) 修正後の X 側 bit-identical 率再測定の中核
debug_timestamp_alignment.py	dump deque 末尾 vs S2 timestamp 目視確認	仮説 I の位相ズレ (= 開始時刻 vs 終了時刻) を 1 dump で目視確認
inspect_ols_dumps.py	dump の sanity check (count/var/値分布)	新規 dry-run で dump 取得する度に毎回使う
compare_3way_ols.py	production CSV vs 学習側 S5/S6 の post-OLS 比較	仮説 IV (数値安定性) 修正後の Y 側検証
analyze_3way_structure.py	compare_3way_ols 結果を engine/TF/window で構造分析	仮説検証結果の構造分析
infer_period.py	既存モデルで OOS 推論	パイプライン構成要素 (= 検証スクリプトではないが再学習で必須)
【保留】 補助・必要なら使う (3 個)
スクリプト	役割	判断
snapshot_training_inference.py	training snapshot 生成 (post-OLS)	compare_3way_ols とセット。 snapshot 再生成が必要なら使う
snapshot_training_inference_with_pre_ols.py	pre-OLS 込み snapshot 生成	同上。 pre-OLS まで遡る検証をするなら使う
compare_snapshots.py	snapshot 同士の比較	snapshot ベース検証をするなら使う
→ 仮説 I-IV は dump ベース (verify_dump_deque_order.py) で進められるので、 snapshot 系は「念のため温存」 のポジション。
【アーカイブ】 役目完了、 記録として残すが能動使用しない (3 個)
スクリプト	役割	判断
verify_s1_m5_timestamp_convention.py	S1 M5 が label="left" を実データ立証	§11.22.4 で立証完了。 結論は確定済なので再実行不要
verify_pre_ols.py	pre-OLS (純化前 engine 計算) の bit-identical 検証	§11.13 で「engine 計算は完璧」 と確定済
verify_pre_ols_v2.py	同上 (v2)	同上
→ これらは「真因が pre-OLS でも timestamp 規約でもなく 2_G の proxy だった」 と確定させた証拠スクリプト。 結論が出ているので今後の能動使用はないが、 削除はせず記録として保持 推奨 (= 後で「なぜ pre-OLS を否定できたのか」 を辿れるように)。
注意: 今後の仮説検証で「足りないもの」
仮説 I-IV の検証では、 上記の検証スクリプトに加えて production コード側の計装 (instrumentation) 追加 が必要です。 これはスクリプトではなく production コードの一時改修:
•	仮説 I: realtime_feature_engine.py の _ffill_lookup_market_proxy 呼び出し前に search_ts を bucket_start にオフセット + ロギング
•	仮説 IV: 同ファイルの var_x 計算箇所に clip 発動回数 + E[X²]/(E[X])²/var_x のロギング
これらは「dry-run + 計装版 production」 で新規 dump を取って、 既存の verify_dump_deque_order.py で検証する、 という流れになります。


emit_train_proxy.pyとverify_dump_deque_order_v2.py

emit_train_proxy.py: 2_G の build_proxy_lazyframe を importlib で逐語流用(式ドリフトゼロ)。HF 各 TF の S2 e1a timestamp 軸(start-time)に join_asof(backward, exact許可)+fill_null(0.0) で proxy を貼り、train_proxy_<TF>.parquet を出力。
verify_dump_deque_order_v2.py: X比較を、この ground-truth との完全一致 timestamp 突合に差し替え(旧 L56 旧式 + merge_asof(allow_exact_matches=False) を撤去)。位置対応(bar-by-bar)は Phase B で cadence 一致済なので維持。Y比較は1バイトも触っていない。
/workspace での実行手順
bash
# 0) 前提: OLS診断パイプラインで 5/2-5/26 の S1/S2 が計算済
#    (run_ols_diagnosis_pipeline.sh で S1_PROCESSED / S2_FEATURES_VALIDATED が 5/26 をカバー)

# 1) 学習側 ground-truth proxy を生成
python /workspace/models/emit_train_proxy.py --two-g /workspace/validation/2_G_alpha_neutralizer.py --out-dir /workspace/data/diagnostics/train_proxy

# 2) v2 で再測定 (X = ground-truth 突合 / Y = 従来どおり)
python /workspace/models/verify_dump_deque_order_v2.py --dump-dir /workspace/logs/ols_state_dumps --train-proxy-dir /workspace/data/diagnostics/train_proxy --out-dir /workspace/data/diagnostics/dump_strict_v2

出てくる数字の読み方(覚悟の確認)
•	M5/M1 の 97% は下がるはず。それは「production 自己整合」だった旧数字が剥がれ、真の train-serve 一致率に置き換わる、という想定どおりの動き。下がること自体が「旧測定が嘘だった」証拠なので、下がって正常です。
•	そのうえで TF パターンを見る: 
o	全TFで一様に低い → 仮説 I(close-vs-start lookup ズレ)が主因確定 → 修正は _close_ts_for/search_ts を学習側と同じ開始時刻に揃える。
o	X が揃うのに Y(compare_y_strict)に巨大乖離(7565 等)が残る → 仮説 IV(var_x の Catastrophic Cancellation) → 共有 Welford へ。
o	X∧Y 両方揃う → proxy 無罪 + 数値安定 OK → deploy 判断へ。
合成検証の M8 8/8 と「同一 ts で旧式≠修正式 12/13」が示すとおり、実データでも X は今より悪化した数字で再表示されると予想します。それが正しい出発点です。


probe_y_singlepoint.py

python /workspace/models/probe_y_singlepoint.py --dump-dir /workspace/logs/ols_state_dumps

何を見るか
各対象特徴量(7565 の M5:e1a_statistical_moment_8_50 等 + 対照群に e1e系)について、deque の末尾からの位置別に |prod − train| を出します:
•	-1(= signal_ts 単点)でも median|diff| が大きい → 特徴量そのものが signal_ts で既に乖離 = 入力バッファ/バー timing が signal_ts 時点で違う(上流確定)。→ 次は (2) 入力 OHLCV バッファ計装へ。
•	-1 は小さいのに -10 / -50 で大きい → deque 取得 timing/cadence の artifact(信号点は合うが履歴の積み方がズレてる)。→ deque 充填ロジックの修正。
•	対照群 e1e_ が全位置で小さい* → 突合ロジック自体は健全、という担保(これが大きいと比較が壊れてる合図)。
さらに「最悪 |diff| がどの位置で出るか」も集計するので、爆発が信号点に集中か履歴全体に分散かが一目で分かります。
想定している分岐
私の読みでは、moment_8 の z⁸ 増幅 + X(proxy)が M5 で合っている事実から、**-1 も含め全位置で爆発(=単点乖離)**が出ると予想します。そうなれば「base-feature の入力バッファ/バー timing が signal_ts 時点で学習側と違う」が確定し、真因は完全に OLS より上流に移ります。


inspect_worst_bar.py
まず私の読み(probe 結果の解釈)を確定させておきます
•	対照群 e1e が全位置 0 = 突合は正しい。「比較が壊れてるから爆発してる」ではない。本物。
•	巨大乖離は signal_ts では起きず、固定された1本のバーに局在(位置帯が deque スライドと同期 = 絶対 timestamp 固定)。→ 「一様な timing ズレ」説は棄却。
•	signal_ts 付近も中程度に非ゼロ(moment_8=0.18 等)= rolling 標準化(z)経路の微小な context 依存が z⁸ で増幅。
•	結論: 真因は base-feature の rolling 計算 + 特定バーの扱いに局在。proxy/OLS/feed ではない。
実行
bash
python /workspace/models/inspect_worst_bar.py --dump-dir /workspace/logs/ols_state_dumps --context 3
出てくるもの・読み方
対象3つ(7565 の M5:e1a_moment_8_50 ほか)について:
1.	最悪 |diff| が出た絶対 timestamp の分布 — 全 dump で同じバーに集中するはず(私の予想)。「最頻 worst bar」が1点に固まれば「固定の1本」確定。
2.	そのバーの prod値 / train値 / close / disc / log_return / rolling_volatility(前後3本も)。
判定の肝(出力末尾に明記してあります):
•	そのバーが disc=True(週末ギャップ等) → disc バーでの rolling リセット/扱いが live と train で違う、が濃厚。
•	train feature も巨大なのに値が違う → 両側が外れ値を見ていて、5σ クリップ境界(EWM)が live と train で違う。
•	train は小さいのに prod だけ巨大 → そのバーで prod の計算/状態(rolling_std=0 で z 発散、warmup 等)が破綻。
•	close 自体が prod と整合しない兆候(train close が異常)→ 入力バー1本が違う。
これでスパイクの機序が確定します。そこが決まれば、修正は「stable_rolling 経路の徹底(両側で同一 z 計算)」か「disc バーの扱い統一」か「5σクリップの境界一致」か、のどれかにピンポイントで絞れます。


probe_gap_bar_mechanism.py
決め手: gap バーの「素の」moment を S1 から再計算して三者突合
stable_moment_k_engine_formula を S1 連続 close に直接当てて gap バーの素値を出し、S2(7680=学習QA後)と dump(115=本番)のどちらに一致するかで M1/M2 を割ります。
ファイルを作成しました, コマンドを実行しました, ファイルを読み取りました
ファイルを作成しました, コマンドを実行しました, ファイルを読み取りました
ここまでの確定事項を整理すると、**真因は「週末ギャップバー(disc=True)の高次特徴量を、学習側 engine と本番でクリップ/扱いが非対称」**まで絞れました。proxy でも OLS でも feed でもない。そしてそのギャップ外れ値が OLS 窓に2日間居座って β/α を壊し、post-OLS 乖離の主犯になっている、という連鎖です。
実行
bash
python /workspace/models/probe_gap_bar_mechanism.py --dump-dir /workspace/logs/ols_state_dumps
(core が blueprint.CORE_DIR にある前提で stable_moment_k_engine_formula を import します。違う場所なら教えてください。)
これで M1/M2 が割れます
gap バーの 素の moment(S1 連続 close に共有関数を直接適用・QAクリップ無し)を出して、S2(7680)と dump(115)のどちらに一致するか:
•	raw ≈ S2(7680)、raw ≠ dump(115) → 学習側は素通し、本番が gap 値をクリップ/抑制 = (M2) 5σ EWM クリップ境界の差。QAState の5年成熟 seed 継承(S3_QA_STATES_DIR)が効き切っていない、が濃厚。
•	raw ≈ dump(115)、raw ≠ S2(7680) → 本番が素値どおり、学習側が増幅。学習 QA 経路が gap で異常。
•	raw が両方と違う → (M1) 50本 close 窓そのものが両側で違う(buffer/warmup の窓差)。
併せて z[gap] / rollmean / rollstd も出すので、115 が「クリップ天井」なのか「素値」なのかが一目で分かります(私の予想は M2 = 本番がクリップしている)。
出力が出たら
•	M2 確定 → 修正は「5σ クリップ境界を両側一致させる」=QAState seed の継承を検証・修正、または学習側にも同じクリップを効かせて再学習。どちらが SSoT 的に正しいかは、blueprint の「engine がギャップ処理」意図に照らすと「両側でギャップを同じく抑制 → 再学習」が筋。
•	M1 確定 → buffer/warmup の窓構築を両側一致させる。


inspect_qastate_seed.py
真因チェーン(現時点の確度)
1.	週末ギャップバー(disc=True, 2026-05-24 22:00)で e1a_moment_8 の生値 = 7680(z=4.98 の本物の5σギャップを z⁸ 増幅)。
2.	学習側は素通し(raw=S2=7680、QAクリップが効かない=連続EWMが gap バー自身で自己膨張し境界が広い)。
3.	production は 115。だがクリップ算数上、生値7680を取り込めば境界≥1880で115にならない → warmup 中の skip_qa_update=True が QA EWM を artifact seed に凍結し、その狭い seed 境界で 7680→115 にクリップした、が最有力(M2′)。
4.	この 7680(学習)vs 115(本番)が OLS 窓に2日居座り β/α を破壊 → post-OLS 乖離の主犯。
残る分岐を artifact を直接読んで確定します:
bash
python /workspace/models/inspect_qastate_seed.py
(blueprint.S3_QA_STATES_DIR を見ます。違えば --qa-dir で指定。)
読み方:
•	seed の 5σ 上限 ≈ 115(σ≈23、狭い) → (M2′)確定。warmup 凍結が真因。 → 修正: warmup で QA EWM を「artifact seed で凍結」ではなく「artifact seed から継続 update」させる(skip_qa_update=False)。seed は学習終端の成熟状態なので、そこから warmup バーを流せば学習の連続 EWM と同一に進化し、gap バーで境界が自己膨張して 7680 を通す = train-serve 一致。再学習不要。
•	seed の上限 > 7680(σ広い) → seed では通すはず → 115 は (M1)生値が違う経路 → production の buffer 窓を精査。
私の予想は M2′(狭い seed × warmup 凍結)。skip_qa_update は Phase D-3 で「30日warmupが5年成熟を壊さないように」入れたものですが、その凍結こそがスパイクバーで境界の自己膨張を止め、学習と乖離させている——保護のつもりが skew を生んでいた、という構図です。


resample_gap_compare.py
なぜ production だけ 115(タ)になっているのかの機序を確定する必要があります。production の 115 が「偶然タになっている」のか「本番が実は正しくギャップを吸収している」のかで、修正の設計が変わるからです。前者なら設計し直し、後者なら本番のロジックを学習側へ移植。
確定済みの事実: 両側とも moment 計算で disc を使っていない(engine_1_A も rfe も)。つまり週末ギャップが z にそのまま流れ込む。なのに値が違う = リサンプル/buffer 構築での窓の違いが濃厚。両側のリサンプル経路を直接読んで源を特定します。
再学習を前提に、本質的な修正方針を検討した。
再学習を前提に、本質的な修正方針を検討した。
リサンプル経路を両側読みました。規約は一致しています:
•	S1(s1_1_B L359): group_by_dynamic(every=freq, closed="left", label="left") + filter(tick_count>0)
•	本番(rfe L1317/L1467): resample(rule, closed="left", label="left") + V=0 GUARD(L1530、s1_1_B の tick_count>0 と物理的に同一を明示)+ 旧 ffill 廃止(L1482)
規約レベルでは揃っている。なのに gap バーの生値が違う(7680/115)。つまり規約のバグではなく、gap 境界での実際のバー内容(どの M0.5 がどの M5 バケットに入るか)の差に絞られます。ここは pandas.resample(本番)と polars.group_by_dynamic(S1)の週末ギャップ跨ぎでのバケット境界(origin/anchoring)の差が出る典型ポイント。静的読みではこれ以上詰められないので、実データで両リサンプルを突合します。
stable_moment_k を両者の M5 close 列に当てて、gap バーで 7680 になるか 115 になるかを見ます(dry-run 不要・既存 S1 のみ)。
ファイルを作成しました, コマンドを実行しました, ファイルを読み取りました
ファイルを作成しました, コマンドを実行しました, ファイルを読み取りました
方針が固まったので、現状をはっきりさせます。
いまの地点
真因は 「週末ギャップ(disc=True)で高次 rolling 特徴量が、両側で違う生値になる」 まで確定。クリップ・pkl・OLS・feed はすべて無罪。リサンプル規約も一致を確認済み。残るのは gap 跨ぎでの実バケット内容の差(pandas.resample vs polars.group_by_dynamic の anchoring)で、ここは静的読みの限界。
bash
python /workspace/models/resample_gap_compare.py
これで dry-run なしに切れます:
•	両者 moment ≈ 7680 で一致 → リサンプルは同一 → 本番の 115 は runtime buffer(warmup/extent)由来 → 計装 dry-run で gap バーの生値+rollstd を直接 capture して確定。
•	gap で close/moment が食い違う → anchoring 差が M1 真因、offline で確定。close 突合行で「どの M0.5 がどの M5 バケットに入るか」が見えます。


probe_buffer_truncation.py
意味すること:
•	production の「コード + リサンプル + 連続データ」は 7680 を出す(= 学習側 = canonical 値)。
•	なのに live dry-run は 115。→ 115 は純粋に runtime buffer の状態アーティファクト。
•	つまり gap バーの moment_8 の正しい値は 7680(モデルはこれを学習した)で、production がそれを再現できていない = production 側のバグ。
オフラインで何度再現しても 7680。115 を出すには production の実行時バッファの中身が要る。残るオフラインの最後の1手は「バッファ長(warmup 境界)で 115 が出るか」のテスト。stable_moment_k が文脈独立なら ≥99本で 7680 のはずですが、production のバッファが gap バー処理時に短ければ 115 になりうる。これを切ります。
ファイルを作成しました, コマンドを実行しました, ファイルを読み取りました
ファイルを作成しました, コマンドを実行しました, ファイルを読み取りました
bash
python /workspace/models/probe_buffer_truncation.py
これがオフライン最後の1手です。gap バーで終わる長さ L のバッファに stable_moment_k を当て、L 毎に末尾値を出します:
•	どこかの L で ≈115 が出る → バッファ長/warmup 境界が真因(オフライン確定、計装不要)。production の data_buffers が gap バー処理時に短かった、で説明完了。
•	≥99 で常に 7680、<99 で NaN/0 のみ(115 が出ない) → 115 は buffer 長でなく中身が違う → ここで初めて計装 dry-run(gap バーでの data_buffers close 配列 + 生 moment を直接 capture)が必要。


inspect_live_buffer.py
計装 dry-run は不要でした。あなたの直感は半分正しく(runtime の中身が要る)、でもその runtime は既に state pickle に保存済みです。
bash
python /workspace/models/inspect_live_buffer.py
(STATE_CHECKPOINT_DIR/feature_engine_state.pkl を読みます。場所が違えば --state で指定。dry-run 終了時の最後の save_state を使います。)
これで完全に確定します
1.	production の実 M5 close バッファで moment_8 を再計算 → 115 か 7680 か。115 なら「production のバッファ close が S1 と違う」が確定。
2.	その close バッファを S1 M5 と位置突合 → どのバーの close が、いくつ違うか(gap バーの Δ を直接表示)。
3.	live M0.5(m05_dataframe)を S1 M0.5 と週末リオープンで突合 → 「live 集計 vs offline S1 集計」の差がバー単位で見える。差があれば週末リオープンのデータ集計差が真因確定。
予想される結末と、それぞれの含意
•	gap バーの close が live と S1 で数ポイント違う(最有力)→ 週末リオープンで EA のリアルタイム M0.5 集計と、オフライン S1 の tick→M0.5 集計がズレている。z⁸ がそれを 7680→115 に増幅。e1e が無傷だったのも(鈍感だから)説明がつく。 → 修正は2本立て: (a) リオープンの M0.5 集計を両側一致させる(データparity)、(b) それでも z⁸ は脆弱なので SSoT で disc バーの z を winsorize + OLS disc 除外して再学習(あなたが許容した根本修正)。
•	M0.5 同一なのに M5 buffer が違う → buffer 構築経路(append/重複排除/gap-fill)の精査へ。
state pickle に M0.5 が残っているかは maxlen 次第なので([4] が空なら別途取得しますが)、少なくとも [1]〜[3] の M5 buffer 突合は必ず効きます。これで「なぜ 115 か」の最後の1ピースが埋まります。
これは大きな再フレームです。予想(リオープンのデータ差)は否定されました。落ち着いて読みます:
1.	live M0.5 = S1 M0.5(リオープンで Δ=0.00000) → データ feed の差は無い。私のリオープン集計差説は棄却。
2.	production の現在の M5 buffer は S1 と 3595/3600 bit-identical。しかも差が出ている5本は live エッジ(5/26 15:25 付近、Δ~0.2)だけで、gap バー(5/24 22:00)は bit-identical。
3.	つまり production の現在の buffer の gap close は正しい(4551.625) → その buffer で moment を再計算すれば(truncation テストより、gap 周辺99本が一致なので)7680 になるはず。
4.	なのに dump の y_deque は gap で 115。
→ 115 は「処理時(5/24)に凍結された stale/transient な値」で、現在の正しい buffer が出す 7680 と一致しない。 OLS deque に積まれた gap バーの特徴量が、計算された瞬間の buffer 状態(warmup/catch-up/スナップショット復帰時)で 115 になり、そのまま凍結された、という構図。バグは**特徴量の式でも現在の buffer でもなく、「処理時に OLS deque へ何が積まれたか」**にある。
これを同じ pickle で確定できます。スクリプトの [3] が tz 比較バグで出ていなかったので直し、さらに state の proxy_feature_buffers(=保存された OLS deque)に積まれた gap の値を読んで、buffer 再計算値と突き合わせます。
ファイルを編集しました, コマンドを実行しました, ファイルを読み取りました
ファイルを編集しました, コマンドを実行しました, ファイルを読み取りました
スクリプトを直しました(同じ pickle で完結)。もう一度:
bash
python /workspace/models/inspect_live_buffer.py
今回の結果の意味(再フレーム)
これまでの消去法で残っていた「データ feed 差」「buffer 中身の差」が両方とも否定されました。M0.5 は完全一致、現在の M5 buffer も gap バーで一致。**だから 115 は『今の buffer から計算した値』ではなく、『5/24 にその瞬間の状態で計算され、OLS deque に凍結された stale 値』**という仮説に絞られます。
修正版が出すのは:
•	[3] 現在 buffer で再計算した gap moment(= 7680 のはず)
•	[3] 保存された OLS deque に積まれている gap 値(= 115 のはず)
この2つが 7680 ≠ 115 と出れば、真因が完全に確定します:
buffer も close も正しい。だが OLS deque に積まれた gap バーの特徴量が、処理時(warmup / catch-up / スナップショット復帰)の populate ロジックで 115 になり凍結された。 = train-serve skew は「特徴量を deque へ積む瞬間の経路」にある。
なぜこれが効くか・次に何を見るか
もし確定したら、調べる先は明確です。warmup / 差分追いつき(warmup_only)で OLS deque(proxy_feature_buffers)に特徴量を populate する経路 — そこで「現在 buffer 全体で再計算」ではなく「その時点の不完全な状態で計算」していないか。特に:
•	スナップショット復帰後の catch-up replay でバーを1本ずつ流す際、buffer がまだ揃う前に feature を積んでいないか
•	warmup の一括充填と、その後の per-bar feature 計算の順序
ちなみに [2] で出た 5/26 15:25 の Δ=0.2(live エッジの5本) は別件の小さな問題(最新バーの形成中/最終 tick 差)で、今回の主犯ではありません。footnote 程度。
出力(特に [3] の2値)を貼ってください。7680 vs 115 が並んで出れば真因確定、そこから populate 経路の該当コードを特定して、修正(+あなたが許容した再学習)に入ります。一歩手前まで来ました。



verify_y_exact_alignment.py
python /workspace/models/verify_y_exact_alignment.py
正直に、いま何が起きているか
私の「pre-OLS が gap で乖離」という診断の土台(positional Y アライメント)が、おそらく間違っていました。 state pickle が示した「OLS deque は gap で正しく 7680 を持つ」が事実なら、115 はずっと比較スクリプトの off-by-oneを見ていただけです。
このスクリプトは2つのアンカーで gap バーを比較します:
•	旧(バグ): label ≤ signal_ts → M3 境界で未確定バーを1本余計に含む → gap で 115(隣)
•	新(正): label + tf_dur ≤ signal_ts → 閉じたバーのみ → gap で 7680(一致)のはず
新アンカーで gap が一致(|diff|→0)すれば、7565 の pre-OLS 乖離は off-by-one アーティファクトで確定、つまり gap バーに本物の pre-OLS skew は無い。
これが意味すること(重い)
もしこれが確定したら、私がこの数ステップで積み上げた「週末ギャップ仮説」のpre-OLS 部分は丸ごとアーティファクトでした。state pickle という独立ソースで突き合わせるまで気づけなかった。probe_y_singlepoint も inspect_worst_bar も同じズレた positional アライメントを共有していたので、互いに整合して見えていた(=確証バイアス)。X が exact merge で 97.8% だったのに Y だけ positional のままだった、という v2 改修時の非対称が穴でした。
だから次は問題を「巻き戻して」再定義します
pre-OLS が実は健全なら、この調査の出発点だった "post-OLS / TF非対称 X-side skew"(Phase 11.27)自体を、正しいアライメントで測り直す必要があります。それも同じ positional ズレに汚染されていた可能性がある。
順序:
1.	まず上を流して off-by-one を確定(gap が新アンカーで一致するか)。
2.	確定したら、全 pre-OLS Y 比較を新アンカーで再実行(verify_dump_deque_order_v2 の Y 側を label+tf_dur<=signal_ts に修正)→ 残差の本物 skew があるか。
3.	その上で 元の post-OLS skew を正しいアライメントで再測定 → そこに本物の skew が残るか。

remeasure_skew_clean.py
remeasure_skew_clean.py できました。これが正しい物差しです。
実行
bash
# Y(pre-OLS)だけなら引数なしで全TF測定
python /workspace/models/remeasure_skew_clean.py

# X(proxy)も測るなら emit_train_proxy の出力dirを渡す
python /workspace/models/remeasure_skew_clean.py \ --train-proxy-dir "$(python -c 'import sys; sys.path.insert(0,"/workspace"); import blueprint as c; print(c.DATA_DIR / "diagnostics" / "train_proxy")')"
何をするか
最新 dump 1本を使い、全 TF × deque 内の全特徴量を「閉じたバー規約(label + tf_dur ≤ signal_ts)」で S2 と exact-timestamp 突合し、TF 毎に:
•	検査セル数(特徴量×バー)
•	真の bit-identical 率
•	最悪 |diff| とその特徴量
を出します。train_proxy dir を渡せば X(market_proxy)も同じ基盤で測ります。
読み方と分岐
•	全 TF が ≈100% → 旧 skew(Y の 7565 も、X の M0.5/M15/M8 ~0% も)は全部アライメント計測アーティファクトだった。deploy ブロッカー消失 = もともと系は揃っていた。
•	特定 TF/特徴量で率が落ちる → そこが初めて本物の調査対象。off-by-one を除いた残差なので、今度は実体のある skew。
特に注目は X 側。旧 merge_asof(allow_exact_matches=False) で M0.5/M15/M8 が ~0% だったやつが、正しいアンカーで何%になるか。ここが deploy 可否を直接左右します。



12 個を「今後の仮説 I-IV 検証で使うか」 で 3 分類しました。
【いる】 今後の検証で能動的に使う中核 (6 個)
スクリプト	役割	今後の用途
verify_dump_deque_order.py	dump x_deque/y_deque vs 学習側の X/Y 比較	仮説 I (位相ズレ) 修正後の X 側 bit-identical 率再測定の中核
debug_timestamp_alignment.py	dump deque 末尾 vs S2 timestamp 目視確認	仮説 I の位相ズレ (= 開始時刻 vs 終了時刻) を 1 dump で目視確認
inspect_ols_dumps.py	dump の sanity check (count/var/値分布)	新規 dry-run で dump 取得する度に毎回使う
compare_3way_ols.py	production CSV vs 学習側 S5/S6 の post-OLS 比較	仮説 IV (数値安定性) 修正後の Y 側検証
analyze_3way_structure.py	compare_3way_ols 結果を engine/TF/window で構造分析	仮説検証結果の構造分析
infer_period.py	既存モデルで OOS 推論	パイプライン構成要素 (= 検証スクリプトではないが再学習で必須)
【保留】 補助・必要なら使う (3 個)
スクリプト	役割	判断
snapshot_training_inference.py	training snapshot 生成 (post-OLS)	compare_3way_ols とセット。 snapshot 再生成が必要なら使う
snapshot_training_inference_with_pre_ols.py	pre-OLS 込み snapshot 生成	同上。 pre-OLS まで遡る検証をするなら使う
compare_snapshots.py	snapshot 同士の比較	snapshot ベース検証をするなら使う
→ 仮説 I-IV は dump ベース (verify_dump_deque_order.py) で進められるので、 snapshot 系は「念のため温存」 のポジション。
【アーカイブ】 役目完了、 記録として残すが能動使用しない (3 個)
スクリプト	役割	判断
verify_s1_m5_timestamp_convention.py	S1 M5 が label="left" を実データ立証	§11.22.4 で立証完了。 結論は確定済なので再実行不要
verify_pre_ols.py	pre-OLS (純化前 engine 計算) の bit-identical 検証	§11.13 で「engine 計算は完璧」 と確定済
verify_pre_ols_v2.py	同上 (v2)	同上
→ これらは「真因が pre-OLS でも timestamp 規約でもなく 2_G の proxy だった」 と確定させた証拠スクリプト。 結論が出ているので今後の能動使用はないが、 削除はせず記録として保持 推奨 (= 後で「なぜ pre-OLS を否定できたのか」 を辿れるように)。
注意: 今後の仮説検証で「足りないもの」
仮説 I-IV の検証では、 上記の検証スクリプトに加えて production コード側の計装 (instrumentation) 追加 が必要です。 これはスクリプトではなく production コードの一時改修:
•	仮説 I: realtime_feature_engine.py の _ffill_lookup_market_proxy 呼び出し前に search_ts を bucket_start にオフセット + ロギング
•	仮説 IV: 同ファイルの var_x 計算箇所に clip 発動回数 + E[X²]/(E[X])²/var_x のロギング
これらは「dry-run + 計装版 production」 で新規 dump を取って、 既存の verify_dump_deque_order.py で検証する、 という流れになります。


emit_train_proxy.pyとverify_dump_deque_order_v2.py

emit_train_proxy.py: 2_G の build_proxy_lazyframe を importlib で逐語流用(式ドリフトゼロ)。HF 各 TF の S2 e1a timestamp 軸(start-time)に join_asof(backward, exact許可)+fill_null(0.0) で proxy を貼り、train_proxy_<TF>.parquet を出力。
verify_dump_deque_order_v2.py: X比較を、この ground-truth との完全一致 timestamp 突合に差し替え(旧 L56 旧式 + merge_asof(allow_exact_matches=False) を撤去)。位置対応(bar-by-bar)は Phase B で cadence 一致済なので維持。Y比較は1バイトも触っていない。
/workspace での実行手順
bash
# 0) 前提: OLS診断パイプラインで 5/2-5/26 の S1/S2 が計算済
#    (run_ols_diagnosis_pipeline.sh で S1_PROCESSED / S2_FEATURES_VALIDATED が 5/26 をカバー)

# 1) 学習側 ground-truth proxy を生成
python /workspace/models/emit_train_proxy.py --two-g /workspace/validation/2_G_alpha_neutralizer.py --out-dir /workspace/data/diagnostics/train_proxy

# 2) v2 で再測定 (X = ground-truth 突合 / Y = 従来どおり)
python /workspace/models/verify_dump_deque_order_v2.py --dump-dir /workspace/logs/ols_state_dumps --train-proxy-dir /workspace/data/diagnostics/train_proxy --out-dir /workspace/data/diagnostics/dump_strict_v2

出てくる数字の読み方(覚悟の確認)
•	M5/M1 の 97% は下がるはず。それは「production 自己整合」だった旧数字が剥がれ、真の train-serve 一致率に置き換わる、という想定どおりの動き。下がること自体が「旧測定が嘘だった」証拠なので、下がって正常です。
•	そのうえで TF パターンを見る: 
o	全TFで一様に低い → 仮説 I(close-vs-start lookup ズレ)が主因確定 → 修正は _close_ts_for/search_ts を学習側と同じ開始時刻に揃える。
o	X が揃うのに Y(compare_y_strict)に巨大乖離(7565 等)が残る → 仮説 IV(var_x の Catastrophic Cancellation) → 共有 Welford へ。
o	X∧Y 両方揃う → proxy 無罪 + 数値安定 OK → deploy 判断へ。
合成検証の M8 8/8 と「同一 ts で旧式≠修正式 12/13」が示すとおり、実データでも X は今より悪化した数字で再表示されると予想します。それが正しい出発点です。


probe_y_singlepoint.py

python /workspace/models/probe_y_singlepoint.py --dump-dir /workspace/logs/ols_state_dumps

何を見るか
各対象特徴量(7565 の M5:e1a_statistical_moment_8_50 等 + 対照群に e1e系)について、deque の末尾からの位置別に |prod − train| を出します:
•	-1(= signal_ts 単点)でも median|diff| が大きい → 特徴量そのものが signal_ts で既に乖離 = 入力バッファ/バー timing が signal_ts 時点で違う(上流確定)。→ 次は (2) 入力 OHLCV バッファ計装へ。
•	-1 は小さいのに -10 / -50 で大きい → deque 取得 timing/cadence の artifact(信号点は合うが履歴の積み方がズレてる)。→ deque 充填ロジックの修正。
•	対照群 e1e_ が全位置で小さい* → 突合ロジック自体は健全、という担保(これが大きいと比較が壊れてる合図)。
さらに「最悪 |diff| がどの位置で出るか」も集計するので、爆発が信号点に集中か履歴全体に分散かが一目で分かります。
想定している分岐
私の読みでは、moment_8 の z⁸ 増幅 + X(proxy)が M5 で合っている事実から、**-1 も含め全位置で爆発(=単点乖離)**が出ると予想します。そうなれば「base-feature の入力バッファ/バー timing が signal_ts 時点で学習側と違う」が確定し、真因は完全に OLS より上流に移ります。


inspect_worst_bar.py
まず私の読み(probe 結果の解釈)を確定させておきます
•	対照群 e1e が全位置 0 = 突合は正しい。「比較が壊れてるから爆発してる」ではない。本物。
•	巨大乖離は signal_ts では起きず、固定された1本のバーに局在(位置帯が deque スライドと同期 = 絶対 timestamp 固定)。→ 「一様な timing ズレ」説は棄却。
•	signal_ts 付近も中程度に非ゼロ(moment_8=0.18 等)= rolling 標準化(z)経路の微小な context 依存が z⁸ で増幅。
•	結論: 真因は base-feature の rolling 計算 + 特定バーの扱いに局在。proxy/OLS/feed ではない。
実行
bash
python /workspace/models/inspect_worst_bar.py --dump-dir /workspace/logs/ols_state_dumps --context 3
出てくるもの・読み方
対象3つ(7565 の M5:e1a_moment_8_50 ほか)について:
1.	最悪 |diff| が出た絶対 timestamp の分布 — 全 dump で同じバーに集中するはず(私の予想)。「最頻 worst bar」が1点に固まれば「固定の1本」確定。
2.	そのバーの prod値 / train値 / close / disc / log_return / rolling_volatility(前後3本も)。
判定の肝(出力末尾に明記してあります):
•	そのバーが disc=True(週末ギャップ等) → disc バーでの rolling リセット/扱いが live と train で違う、が濃厚。
•	train feature も巨大なのに値が違う → 両側が外れ値を見ていて、5σ クリップ境界(EWM)が live と train で違う。
•	train は小さいのに prod だけ巨大 → そのバーで prod の計算/状態(rolling_std=0 で z 発散、warmup 等)が破綻。
•	close 自体が prod と整合しない兆候(train close が異常)→ 入力バー1本が違う。
これでスパイクの機序が確定します。そこが決まれば、修正は「stable_rolling 経路の徹底(両側で同一 z 計算)」か「disc バーの扱い統一」か「5σクリップの境界一致」か、のどれかにピンポイントで絞れます。


probe_gap_bar_mechanism.py
決め手: gap バーの「素の」moment を S1 から再計算して三者突合
stable_moment_k_engine_formula を S1 連続 close に直接当てて gap バーの素値を出し、S2(7680=学習QA後)と dump(115=本番)のどちらに一致するかで M1/M2 を割ります。
ファイルを作成しました, コマンドを実行しました, ファイルを読み取りました
ファイルを作成しました, コマンドを実行しました, ファイルを読み取りました
ここまでの確定事項を整理すると、**真因は「週末ギャップバー(disc=True)の高次特徴量を、学習側 engine と本番でクリップ/扱いが非対称」**まで絞れました。proxy でも OLS でも feed でもない。そしてそのギャップ外れ値が OLS 窓に2日間居座って β/α を壊し、post-OLS 乖離の主犯になっている、という連鎖です。
実行
bash
python /workspace/models/probe_gap_bar_mechanism.py --dump-dir /workspace/logs/ols_state_dumps
(core が blueprint.CORE_DIR にある前提で stable_moment_k_engine_formula を import します。違う場所なら教えてください。)
これで M1/M2 が割れます
gap バーの 素の moment(S1 連続 close に共有関数を直接適用・QAクリップ無し)を出して、S2(7680)と dump(115)のどちらに一致するか:
•	raw ≈ S2(7680)、raw ≠ dump(115) → 学習側は素通し、本番が gap 値をクリップ/抑制 = (M2) 5σ EWM クリップ境界の差。QAState の5年成熟 seed 継承(S3_QA_STATES_DIR)が効き切っていない、が濃厚。
•	raw ≈ dump(115)、raw ≠ S2(7680) → 本番が素値どおり、学習側が増幅。学習 QA 経路が gap で異常。
•	raw が両方と違う → (M1) 50本 close 窓そのものが両側で違う(buffer/warmup の窓差)。
併せて z[gap] / rollmean / rollstd も出すので、115 が「クリップ天井」なのか「素値」なのかが一目で分かります(私の予想は M2 = 本番がクリップしている)。
出力が出たら
•	M2 確定 → 修正は「5σ クリップ境界を両側一致させる」=QAState seed の継承を検証・修正、または学習側にも同じクリップを効かせて再学習。どちらが SSoT 的に正しいかは、blueprint の「engine がギャップ処理」意図に照らすと「両側でギャップを同じく抑制 → 再学習」が筋。
•	M1 確定 → buffer/warmup の窓構築を両側一致させる。


inspect_qastate_seed.py
真因チェーン(現時点の確度)
1.	週末ギャップバー(disc=True, 2026-05-24 22:00)で e1a_moment_8 の生値 = 7680(z=4.98 の本物の5σギャップを z⁸ 増幅)。
2.	学習側は素通し(raw=S2=7680、QAクリップが効かない=連続EWMが gap バー自身で自己膨張し境界が広い)。
3.	production は 115。だがクリップ算数上、生値7680を取り込めば境界≥1880で115にならない → warmup 中の skip_qa_update=True が QA EWM を artifact seed に凍結し、その狭い seed 境界で 7680→115 にクリップした、が最有力(M2′)。
4.	この 7680(学習)vs 115(本番)が OLS 窓に2日居座り β/α を破壊 → post-OLS 乖離の主犯。
残る分岐を artifact を直接読んで確定します:
bash
python /workspace/models/inspect_qastate_seed.py
(blueprint.S3_QA_STATES_DIR を見ます。違えば --qa-dir で指定。)
読み方:
•	seed の 5σ 上限 ≈ 115(σ≈23、狭い) → (M2′)確定。warmup 凍結が真因。 → 修正: warmup で QA EWM を「artifact seed で凍結」ではなく「artifact seed から継続 update」させる(skip_qa_update=False)。seed は学習終端の成熟状態なので、そこから warmup バーを流せば学習の連続 EWM と同一に進化し、gap バーで境界が自己膨張して 7680 を通す = train-serve 一致。再学習不要。
•	seed の上限 > 7680(σ広い) → seed では通すはず → 115 は (M1)生値が違う経路 → production の buffer 窓を精査。
私の予想は M2′(狭い seed × warmup 凍結)。skip_qa_update は Phase D-3 で「30日warmupが5年成熟を壊さないように」入れたものですが、その凍結こそがスパイクバーで境界の自己膨張を止め、学習と乖離させている——保護のつもりが skew を生んでいた、という構図です。


resample_gap_compare.py
なぜ production だけ 115(タ)になっているのかの機序を確定する必要があります。production の 115 が「偶然タになっている」のか「本番が実は正しくギャップを吸収している」のかで、修正の設計が変わるからです。前者なら設計し直し、後者なら本番のロジックを学習側へ移植。
確定済みの事実: 両側とも moment 計算で disc を使っていない(engine_1_A も rfe も)。つまり週末ギャップが z にそのまま流れ込む。なのに値が違う = リサンプル/buffer 構築での窓の違いが濃厚。両側のリサンプル経路を直接読んで源を特定します。
再学習を前提に、本質的な修正方針を検討した。
再学習を前提に、本質的な修正方針を検討した。
リサンプル経路を両側読みました。規約は一致しています:
•	S1(s1_1_B L359): group_by_dynamic(every=freq, closed="left", label="left") + filter(tick_count>0)
•	本番(rfe L1317/L1467): resample(rule, closed="left", label="left") + V=0 GUARD(L1530、s1_1_B の tick_count>0 と物理的に同一を明示)+ 旧 ffill 廃止(L1482)
規約レベルでは揃っている。なのに gap バーの生値が違う(7680/115)。つまり規約のバグではなく、gap 境界での実際のバー内容(どの M0.5 がどの M5 バケットに入るか)の差に絞られます。ここは pandas.resample(本番)と polars.group_by_dynamic(S1)の週末ギャップ跨ぎでのバケット境界(origin/anchoring)の差が出る典型ポイント。静的読みではこれ以上詰められないので、実データで両リサンプルを突合します。
stable_moment_k を両者の M5 close 列に当てて、gap バーで 7680 になるか 115 になるかを見ます(dry-run 不要・既存 S1 のみ)。
ファイルを作成しました, コマンドを実行しました, ファイルを読み取りました
ファイルを作成しました, コマンドを実行しました, ファイルを読み取りました
方針が固まったので、現状をはっきりさせます。
いまの地点
真因は 「週末ギャップ(disc=True)で高次 rolling 特徴量が、両側で違う生値になる」 まで確定。クリップ・pkl・OLS・feed はすべて無罪。リサンプル規約も一致を確認済み。残るのは gap 跨ぎでの実バケット内容の差(pandas.resample vs polars.group_by_dynamic の anchoring)で、ここは静的読みの限界。
bash
python /workspace/models/resample_gap_compare.py
これで dry-run なしに切れます:
•	両者 moment ≈ 7680 で一致 → リサンプルは同一 → 本番の 115 は runtime buffer(warmup/extent)由来 → 計装 dry-run で gap バーの生値+rollstd を直接 capture して確定。
•	gap で close/moment が食い違う → anchoring 差が M1 真因、offline で確定。close 突合行で「どの M0.5 がどの M5 バケットに入るか」が見えます。


probe_buffer_truncation.py
意味すること:
•	production の「コード + リサンプル + 連続データ」は 7680 を出す(= 学習側 = canonical 値)。
•	なのに live dry-run は 115。→ 115 は純粋に runtime buffer の状態アーティファクト。
•	つまり gap バーの moment_8 の正しい値は 7680(モデルはこれを学習した)で、production がそれを再現できていない = production 側のバグ。
オフラインで何度再現しても 7680。115 を出すには production の実行時バッファの中身が要る。残るオフラインの最後の1手は「バッファ長(warmup 境界)で 115 が出るか」のテスト。stable_moment_k が文脈独立なら ≥99本で 7680 のはずですが、production のバッファが gap バー処理時に短ければ 115 になりうる。これを切ります。
ファイルを作成しました, コマンドを実行しました, ファイルを読み取りました
ファイルを作成しました, コマンドを実行しました, ファイルを読み取りました
bash
python /workspace/models/probe_buffer_truncation.py
これがオフライン最後の1手です。gap バーで終わる長さ L のバッファに stable_moment_k を当て、L 毎に末尾値を出します:
•	どこかの L で ≈115 が出る → バッファ長/warmup 境界が真因(オフライン確定、計装不要)。production の data_buffers が gap バー処理時に短かった、で説明完了。
•	≥99 で常に 7680、<99 で NaN/0 のみ(115 が出ない) → 115 は buffer 長でなく中身が違う → ここで初めて計装 dry-run(gap バーでの data_buffers close 配列 + 生 moment を直接 capture)が必要。


inspect_live_buffer.py
計装 dry-run は不要でした。あなたの直感は半分正しく(runtime の中身が要る)、でもその runtime は既に state pickle に保存済みです。
bash
python /workspace/models/inspect_live_buffer.py
(STATE_CHECKPOINT_DIR/feature_engine_state.pkl を読みます。場所が違えば --state で指定。dry-run 終了時の最後の save_state を使います。)
これで完全に確定します
1.	production の実 M5 close バッファで moment_8 を再計算 → 115 か 7680 か。115 なら「production のバッファ close が S1 と違う」が確定。
2.	その close バッファを S1 M5 と位置突合 → どのバーの close が、いくつ違うか(gap バーの Δ を直接表示)。
3.	live M0.5(m05_dataframe)を S1 M0.5 と週末リオープンで突合 → 「live 集計 vs offline S1 集計」の差がバー単位で見える。差があれば週末リオープンのデータ集計差が真因確定。
予想される結末と、それぞれの含意
•	gap バーの close が live と S1 で数ポイント違う(最有力)→ 週末リオープンで EA のリアルタイム M0.5 集計と、オフライン S1 の tick→M0.5 集計がズレている。z⁸ がそれを 7680→115 に増幅。e1e が無傷だったのも(鈍感だから)説明がつく。 → 修正は2本立て: (a) リオープンの M0.5 集計を両側一致させる(データparity)、(b) それでも z⁸ は脆弱なので SSoT で disc バーの z を winsorize + OLS disc 除外して再学習(あなたが許容した根本修正)。
•	M0.5 同一なのに M5 buffer が違う → buffer 構築経路(append/重複排除/gap-fill)の精査へ。
state pickle に M0.5 が残っているかは maxlen 次第なので([4] が空なら別途取得しますが)、少なくとも [1]〜[3] の M5 buffer 突合は必ず効きます。これで「なぜ 115 か」の最後の1ピースが埋まります。
これは大きな再フレームです。予想(リオープンのデータ差)は否定されました。落ち着いて読みます:
1.	live M0.5 = S1 M0.5(リオープンで Δ=0.00000) → データ feed の差は無い。私のリオープン集計差説は棄却。
2.	production の現在の M5 buffer は S1 と 3595/3600 bit-identical。しかも差が出ている5本は live エッジ(5/26 15:25 付近、Δ~0.2)だけで、gap バー(5/24 22:00)は bit-identical。
3.	つまり production の現在の buffer の gap close は正しい(4551.625) → その buffer で moment を再計算すれば(truncation テストより、gap 周辺99本が一致なので)7680 になるはず。
4.	なのに dump の y_deque は gap で 115。
→ 115 は「処理時(5/24)に凍結された stale/transient な値」で、現在の正しい buffer が出す 7680 と一致しない。 OLS deque に積まれた gap バーの特徴量が、計算された瞬間の buffer 状態(warmup/catch-up/スナップショット復帰時)で 115 になり、そのまま凍結された、という構図。バグは**特徴量の式でも現在の buffer でもなく、「処理時に OLS deque へ何が積まれたか」**にある。
これを同じ pickle で確定できます。スクリプトの [3] が tz 比較バグで出ていなかったので直し、さらに state の proxy_feature_buffers(=保存された OLS deque)に積まれた gap の値を読んで、buffer 再計算値と突き合わせます。
ファイルを編集しました, コマンドを実行しました, ファイルを読み取りました
ファイルを編集しました, コマンドを実行しました, ファイルを読み取りました
スクリプトを直しました(同じ pickle で完結)。もう一度:
bash
python /workspace/models/inspect_live_buffer.py
今回の結果の意味(再フレーム)
これまでの消去法で残っていた「データ feed 差」「buffer 中身の差」が両方とも否定されました。M0.5 は完全一致、現在の M5 buffer も gap バーで一致。**だから 115 は『今の buffer から計算した値』ではなく、『5/24 にその瞬間の状態で計算され、OLS deque に凍結された stale 値』**という仮説に絞られます。
修正版が出すのは:
•	[3] 現在 buffer で再計算した gap moment(= 7680 のはず)
•	[3] 保存された OLS deque に積まれている gap 値(= 115 のはず)
この2つが 7680 ≠ 115 と出れば、真因が完全に確定します:
buffer も close も正しい。だが OLS deque に積まれた gap バーの特徴量が、処理時(warmup / catch-up / スナップショット復帰)の populate ロジックで 115 になり凍結された。 = train-serve skew は「特徴量を deque へ積む瞬間の経路」にある。
なぜこれが効くか・次に何を見るか
もし確定したら、調べる先は明確です。warmup / 差分追いつき(warmup_only)で OLS deque(proxy_feature_buffers)に特徴量を populate する経路 — そこで「現在 buffer 全体で再計算」ではなく「その時点の不完全な状態で計算」していないか。特に:
•	スナップショット復帰後の catch-up replay でバーを1本ずつ流す際、buffer がまだ揃う前に feature を積んでいないか
•	warmup の一括充填と、その後の per-bar feature 計算の順序
ちなみに [2] で出た 5/26 15:25 の Δ=0.2(live エッジの5本) は別件の小さな問題(最新バーの形成中/最終 tick 差)で、今回の主犯ではありません。footnote 程度。
出力(特に [3] の2値)を貼ってください。7680 vs 115 が並んで出れば真因確定、そこから populate 経路の該当コードを特定して、修正(+あなたが許容した再学習)に入ります。一歩手前まで来ました。



verify_y_exact_alignment.py
python /workspace/models/verify_y_exact_alignment.py
正直に、いま何が起きているか
私の「pre-OLS が gap で乖離」という診断の土台(positional Y アライメント)が、おそらく間違っていました。 state pickle が示した「OLS deque は gap で正しく 7680 を持つ」が事実なら、115 はずっと比較スクリプトの off-by-oneを見ていただけです。
このスクリプトは2つのアンカーで gap バーを比較します:
•	旧(バグ): label ≤ signal_ts → M3 境界で未確定バーを1本余計に含む → gap で 115(隣)
•	新(正): label + tf_dur ≤ signal_ts → 閉じたバーのみ → gap で 7680(一致)のはず
新アンカーで gap が一致(|diff|→0)すれば、7565 の pre-OLS 乖離は off-by-one アーティファクトで確定、つまり gap バーに本物の pre-OLS skew は無い。
これが意味すること(重い)
もしこれが確定したら、私がこの数ステップで積み上げた「週末ギャップ仮説」のpre-OLS 部分は丸ごとアーティファクトでした。state pickle という独立ソースで突き合わせるまで気づけなかった。probe_y_singlepoint も inspect_worst_bar も同じズレた positional アライメントを共有していたので、互いに整合して見えていた(=確証バイアス)。X が exact merge で 97.8% だったのに Y だけ positional のままだった、という v2 改修時の非対称が穴でした。
だから次は問題を「巻き戻して」再定義します
pre-OLS が実は健全なら、この調査の出発点だった "post-OLS / TF非対称 X-side skew"(Phase 11.27)自体を、正しいアライメントで測り直す必要があります。それも同じ positional ズレに汚染されていた可能性がある。
順序:
1.	まず上を流して off-by-one を確定(gap が新アンカーで一致するか)。
2.	確定したら、全 pre-OLS Y 比較を新アンカーで再実行(verify_dump_deque_order_v2 の Y 側を label+tf_dur<=signal_ts に修正)→ 残差の本物 skew があるか。
3.	その上で 元の post-OLS skew を正しいアライメントで再測定 → そこに本物の skew が残るか。

remeasure_skew_clean.py
remeasure_skew_clean.py できました。これが正しい物差しです。
実行
bash
# Y(pre-OLS)だけなら引数なしで全TF測定
python /workspace/models/remeasure_skew_clean.py

# X(proxy)も測るなら emit_train_proxy の出力dirを渡す
python /workspace/models/remeasure_skew_clean.py \ --train-proxy-dir "$(python -c 'import sys; sys.path.insert(0,"/workspace"); import blueprint as c; print(c.DATA_DIR / "diagnostics" / "train_proxy")')"
何をするか
最新 dump 1本を使い、全 TF × deque 内の全特徴量を「閉じたバー規約(label + tf_dur ≤ signal_ts)」で S2 と exact-timestamp 突合し、TF 毎に:
•	検査セル数(特徴量×バー)
•	真の bit-identical 率
•	最悪 |diff| とその特徴量
を出します。train_proxy dir を渡せば X(market_proxy)も同じ基盤で測ります。
読み方と分岐
•	全 TF が ≈100% → 旧 skew(Y の 7565 も、X の M0.5/M15/M8 ~0% も)は全部アライメント計測アーティファクトだった。deploy ブロッカー消失 = もともと系は揃っていた。
•	特定 TF/特徴量で率が落ちる → そこが初めて本物の調査対象。off-by-one を除いた残差なので、今度は実体のある skew。
特に注目は X 側。旧 merge_asof(allow_exact_matches=False) で M0.5/M15/M8 が ~0% だったやつが、正しいアンカーで何%になるか。ここが deploy 可否を直接左右します。



verify_proxy_exact.py
本質的な問題(X も Y も同根)
ここまでの全比較は、deque がタイムスタンプを持たないため、cutoff や positional でアライメントを推測している。だから1本ズレが消えない。Y も「窓のどこかで deque と S1/S2 のバー数が1本でも食い違えば、そこから手前が全部ズレる」= 密な M0.5 ほど境界が多く悪化(7%)、疎な M15 が一番マシ(85%)、という観測パターンと完全に整合。positional 推測の限界です。
→ もう推測はやめて、deque 要素の実タイムスタンプで exact 突合するしかない。手段を確認します。dump の market_proxy_tail_200 がタイムスタンプ付きなら、少なくとも proxy はそれで厳密に解けます。計測アーティファクトの本質を認識し、決定的な検証戦略を構想した。計測アーティファクトの本質を認識し、決定的な検証戦略を構想した。重要な発見: market_proxy_tail_200 は g_market_proxy.tail(200).reset_index().to_dict("records")(main.py L1502)= タイムスタンプ付き。これで proxy を推測なしの exact-timestamp で突合できます。しかも全 dump に既に入っている = 再実行不要。
proxy の値が exact timestamp で一致するなら、「X skew」は完全にインデックス(close-vs-start 時刻)のズレで、値の乖離ではないことが確定します。書きます。ファイルを作成しました, コマンドを実行しました, ファイルを読み取りましたファイルを作成しました, コマンドを実行しました, ファイルを読み取りましたbashpython /workspace/models/verify_proxy_exact.py
(再実行不要・全 dump の market_proxy_tail_200 を集約 → train_proxy_<TF> と完全一致 timestamp のみで inner join。)
これで X が決着します

bit-identical ≈ 100% → proxy 値は production と学習で同一。「X skew(全TF ~0%)」は close-vs-start のインデックスズレだけで、値の乖離ではない = 計測アーティファクト。元の M5/M1=97% も、私の cutoff の全 0% も、ただの1本ズレ違いだったと確定。
不一致 → proxy 値に本物の差 → そこを追う。

いま見えてきている全体像(正直に)
ここまでの独立した裏取りを並べると、**「deploy を止めていた skew は、ほぼ全部アライメント/インデックスの計測アーティファクト」**という像が強まっています:

Y の gap 7565 → off-by-one(state pickle で deque は正しく 7680 と確認済)
X の全TF ~0% → 全TF 一致の 0.0095 = 1本ズレの指紋、proxy 源は M5/M1 で 97% 一致実績
根本: deque がタイムスタンプを持たず、cutoff/positional で推測していたのが全ての元凶

もし proxy も exact で一致したら、残るのは「正しい物差しの確立」だけになります。その場合の次の一手は明確です:

proxy/feature deque を dump する時に、並行して bar timestamp も保存する(production の dump フックに timestamp deque を1行追加)。これで今後の全比較が推測ゼロの exact-timestamp になる。FORGE_DEQ_TRACE_TARGETS フックが既にあるので流用できる。
その exact 基盤で全 TF を測り直し、真に残る skew があるかを最終判定 → 無ければ deploy ブロッカー解除。

補足
万一どの train_proxy とも共通 timestamp が出なければ(g_market_proxy の cadence と TF がズレている場合)、train_proxy_M0.5.parquet あたりの timestamp 間隔を一度見せてください — g_market_proxy の native cadence に合わせて突合先を選び直します。


remeasure_from_state.py
正直に言うと、ここまでの dump ベースの比較は全部「タイムスタンプ無し deque を推測アライメント」しているせいで、手法ごとに数字がバラバラ(Y gap=幻、Y全バー=79%、X cutoff=0%/0.0095、X exact=0%/0.0006)。これ以上、推測ベースの計測を増やしても収束しません。
信頼できる唯一の地盤に切り替える
state pickle が最も信頼できるグラウンドトゥルースです。理由:

last_bar_timestamps[tf] で各TFの正確な最終バー時刻が分かる(M3 signal_ts の境界ズレが無い)
data_buffers[tf]["close"] = 生の入力 close(z⁸ 増幅なし) → 1本ズレがあれば多数のバーが一斉に食い違うので、ドリフトの有無が一目で分かる
実際 M5 は inspect_live_buffer で 3595/3600 bit-identical(差は live エッジ5本だけ)= ドリフト無し・入力は正しいと既に判明

これを全 TF の buffer close に拡張します。これが「推測なし」で測れる唯一の量です。ファイルを作成しました, コマンドを実行しました, ファイルを読み取りましたファイルを作成しました, コマンドを実行しました, ファイルを読み取りましたbashpython /workspace/models/remeasure_from_state.py
これで「ドリフトが本物か」が一発で分かります
全 TF の data_buffers[tf]["close"](生の入力)を S1 と突合し、差のあるバーが「末尾(live エッジ)集中」か「中間に散在」かを判定します。close は z⁸ 増幅が無いので、もし1本でもバー列がズレていれば多数の close が一斉に食い違う = ドリフトが即見える。

全 TF で差が末尾集中(M5 の 3595/3600 のように) → 入力 buffer は全 TF 正しい、ドリフト無し。= これまでの skew(7565、X の 0%、Y の 79%)は全部、推測アライメントの計測アーティファクト。系は最初から揃っていた。
どこかの TF で差が中間に散在 → そのTFに本物のバー列ズレ = 初めての実体。そこだけを追えばいい。

今の立ち位置(正直なまとめ)
ここまでの独立した検証を総合すると、強く示唆されているのは:

deploy を止めていた "skew" は、ほぼ計測側のアライメント問題だった。 特徴量の式は文脈独立で正しく(truncation テスト)、OLS deque は gap で正しい値を持ち(state pickle)、入力 M0.5 も buffer も S1 と一致(M5 で実証)。バラバラな数字は全部、ts 無し deque を cutoff/positional で推測した副産物。

この buffer 突合で全 TF が「末尾集中」なら、残作業は2つだけになります:

計測の正常化: dump フックに deque の bar timestamp を1本足し(FORGE_DEQ_TRACE 流用可)、今後は推測ゼロの exact 比較に。
live エッジの5本(M5 で見えた Δ~0.2): 最新の形成中バー/最終 tick の扱い — これだけは小さな実体の可能性。deploy 前に詰める価値あり。


diagnose_m05.py
読み取り

M5 / M8 / M15: クリーン(差は live エッジのみ、5/5・2/2・2/2)。入力 buffer は正しく、ドリフト無し。→ 元の「M15/M8 ~0% skew」はアライメント計測アーティファクト確定。
M3: ほぼクリーン(99.92%、中間1本)。M1: 99.58%(中間~12本、Δ最大0.36)。軽微。
M0.5: 3.61%!(3470本が乖離、エッジ集中ではなく全体に散在、max Δ=24.5)← これが唯一の実体ある標的。

つまり長い迷走の末、本物の問題は M0.5 ただ1つに絞られました。高位足は全部クリーン。
ただし M0.5 も「私のアンカーずれ」の可能性は残す
矛盾があります: 先の inspect_live_buffer [4] で m05_dataframe(タイムスタンプ付き)は S1 M0.5 とリオープンで Δ=0(一致)。なのに data_buffers"M0.5"は96%乖離。これは:

(A) data_buffers["M0.5"] のバー列が m05_dataframe / S1 とズレている(buffer 充填経路の問題 = 本物)、か
(B) 私が last_bar_ts で取った S1 アンカーが M0.5 で外れている(また計測側)

を切り分ける必要があります。決め手は「production 自身の timestamped な m05_dataframe」を基準にすること。
(注: M0.5 buffer は 3600本 = 30時間ぶんで、週末ギャップ(5/24)は窓外。なのでギャップとは無関係の、平常時の M0.5 バー列ズレです。)ファイルを作成しました, コマンドを実行しました, ファイルを読み取りましたファイルを作成しました, コマンドを実行しました, ファイルを読み取りましたbashpython /workspace/models/diagnose_m05.py
長い迷走を経て、実体ある標的は M0.5 ただ1つに絞れました。これを正体まで詰めます(state pickle のみ・dry-run 不要)。3段で:

data_buffers[M0.5] vs m05_dataframe(production 自身、位置突合)— production 内部でバー列が一致しているか。ズレてれば充填経路の問題。
m05_dataframe vs S1 M0.5(exact-timestamp、全 buffer 窓)— M0.5 source が S1 と窓全域で合うか(リオープンだけでなく)。「m05 にあって S1 に無い ts」が出れば余分バー(V=0 等)混入。
data_buffers[M0.5] を m05 の ts で時刻付け → S1 と exact 突合 — これが最終判定:

exact-ts で一致 → (B) remeasure の位置アンカーが M0.5 で外れていただけ。M0.5 buffer も実は正しく、全 TF クリーン = skew は全部計測の幻だった。
exact-ts でも乖離 → (A) M0.5 buffer の値/列が本物で S1 と違う。[1][2] のどちらでズレたかで原因箇所が確定。



現時点の全体像(正直に、そして前向きに)
ここまでで確実に言えること:

高位足(M5/M8/M15)の buffer は完全にクリーン — 元の deploy ブロッカー「M15/M8 ~0%」は計測アライメントの幻だった。
残るのは M0.5 の挙動1点のみ。それが (A) 本物か (B) また計測かを、この diagnose_m05 が確定させる。

(B)なら deploy ブロッカーは完全消滅、調査はクローズ。(A)なら 初めての・唯一の・実体ある修正対象が M0.5 buffer 充填経路に確定し、そこだけ直せばいい。どちらに転んでも、今度こそ結論です。



characterize_m05_drift.py
確定した事実

[2] m05_dataframe(production の生 M0.5)= S1 M0.5 が共通 timestamp で 100% 一致(max Δ 0.36)。→ production が受け取る M0.5 source は正しい。「419719 本が S1 に無い」は単に m05_dataframe が9ヶ月分(2025-08〜)持つ一方、S1 M0.5 診断データが直近窓だけ、という期間差(余分バーではない)。
[1] data_buffers[M0.5] が m05_dataframe と ~98本(2.7%)食い違う(97.28%、末尾から約203本目あたりに集中、エッジではない)。
M5/M8/M15 は完全クリーン。

結論: 唯一の実体は「M0.5 の feature buffer 充填経路」
source(m05_dataframe)は正しいのに、feature 計算用の data_buffers[M0.5] だけが ~98本ズレている。原因は _append_bar_to_buffer の充填/重複排除(dedup)経路 か warmup の一括充填と live の境界。位置203付近に集中 = warmup→live 境界の疑いが濃厚。
注意信号もあります: [2] が 111937/111927 と >100% = m05_dataframe に重複 timestamp が存在(同じバーが複数回 append)。dedup の有無で data_buffers と m05 がズレる、という機序と整合。
影響範囲(朗報)
これは M0.5 features にしか効かない(高位足は resample がクロック境界で吸収するのでクリーン)。元の「TF非対称 deploy ブロッカー」は、大半が計測アライメントの幻 + M0.5 buffer 充填の局所バグ1点に分解された。謎の系統的 skew ではなく、特定コード経路の局所問題まで縮んだ。
最後に、その ~98本がどこで・なぜズレるかを正確に特定します。ファイルを作成しました, コマンドを実行しました, ファイルを読み取りましたファイルを作成しました, コマンドを実行しました, ファイルを読み取りましたbashpython /workspace/models/characterize_m05_drift.py
これで M0.5 の ~98本ズレの正体と発生箇所が確定します:

m05_dataframe の重複 timestamp 数([2] の >100% の原因)。重複があれば、その例も表示。
乖離バーの timestamp 範囲と連続性 — 隣接 index が連続なら warmup 境界/一括充填の区間、散在なら dedup/欠落。
乖離開始境界の前後を data_buffers / S1 の close で並べて表示 → 機序が目視で分かる。

どう転んでも、これでクロージング判断ができます
長い旅でしたが、現在地は明確です:

元の「TF非対称 deploy ブロッカー」は、(1) 大半が ts無し deque を推測アライメントした計測アーティファクト + (2) M0.5 の feature buffer 充填経路の局所ズレ(~2.7%、source は正しい)、に分解された。 高位足(M5/M8/M15)は完全クリーン。

この characterize の結果で:

連続領域(warmup 境界) → 修正は「warmup 一括充填が live と同じバー列・dedup を使うようにする」1点。M0.5 features のみ再生成 → 必要なら再学習(あなたは許容済)。
散在(dedup/欠落) → _append_bar_to_buffer の重複排除ロジックを S1 の filter(tick_count>0) と完全一致させる1点。

そして計測の正常化(dump に deque の bar timestamp を1本足す)を入れれば、今後この種の幻に振り回されることはなくなります。


verify_m05_dedup_exact.py
念のため、重複を除いた exact-timestamp で M0.5 を最終確認します。ここで ≈100% なら「M0.5 も値は完全に正しく、96%は重複の位置ズレ」が確定 = skew は全て幻で決着。ファイルを作成しました, コマンドを実行しました, ファイルを読み取りましたファイルを作成しました, コマンドを実行しました, ファイルを読み取りましたbashpython /workspace/models/verify_m05_dedup_exact.py
重複を除いた exact-timestamp で M0.5 を突き合わせます。≈100% なら「M0.5 の値も完全に正しく、96%は92本の重複が起こした位置ズレ」が確定 = 本物の skew は無し、deploy ブロッカーは全部幻で決着します。


find_m05_offset.py
仮定ゼロの検定: 値が S1 のものか(ズレてるだけ)、本当に別物か
data_buffers[M0.5] の値が S1 の値集合に存在するか(= 実在する S1 価格を並べ替え/シフトしただけ)、それとも どのラグでも S1 に一致しない別データかを、timestamp を使わず判定します。ファイルを作成しました, コマンドを実行しました, ファイルを読み取りましたファイルを作成しました, コマンドを実行しました, ファイルを読み取りましたbashpython /workspace/models/find_m05_offset.py
これは timestamp 仮定をゼロにした最終決着テストです。2つの問いに答えます:

(A) data_buffers[M0.5] の値が S1 の値集合に存在するか — ほぼ100%存在なら「値は本物の S1 価格、並べ方/位置の問題」。過半が不在なら「M0.5 buffer に別データ/偽値=本物の問題」。
(B) 最良ラグ探索 — data_buffers を S1 にスライドして、あるラグで ~一致するか。一致するなら「単に N 本ずれているだけ」=また計測/アンカー問題。

3通りの結末と意味

(A)≈100% & (B) あるラグで一致 → data_buffers は S1 を N 本ずらしただけ。値は完全に正しく、また位置オフセット = 計測の幻。M0.5 も無罪、skew は全部幻で決着。
(A)≈100% だが (B) 単一ラグで揃わない → 値は全部 S1 由来だが順序が非単調に乱れている(dedup/挿入の仕方)。実害は要評価だが、値自体は本物。
(A) 過半が不在 → M0.5 buffer に本物のデータ差(偽値混入)。初めての・唯一の実体ある修正対象。

正直な現状認識
ここまで来て、data_buffers[M0.5] が timestamp を持たないことが最後の壁になっています。私の reconstruct はこの1点で繰り返し滑った。この (A)/(B) は値ベースなのでその壁を回避します。
結果がどう出ても、これで M0.5 は確定します。そして同時に、はっきりした教訓が出ました — どの結末でも、恒久対策は「deque/buffer に bar timestamp を保存する」こと(FORGE_DEQ_TRACE フック流用)。それさえ入れれば、この種の「値は正しいのに位置で測れない」迷路は二度と起きません。