正しい「一括Undo」の方法
ビル全体の命令を取り消すには、「館内放送」を使う必要があります。VS Codeにおける館内放送がコマンドパレットです。

一括置換を実行した直後に、キーボードで Ctrl + Shift + P (Macの場合は Cmd + Shift + P) を押して、コマンドパレットを開きます。

入力欄に Undo と入力します。

候補の中に、ただUndoとだけ書かれた項目（workbench.action.undo）が表示されます。これを選択してEnterキーを押してください。