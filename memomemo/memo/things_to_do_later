######################################################################################
feature_validator.py - パーミュテーションの近似
pythonshuffled_col = X1_val[feature].map_partitions(lambda s: s.sample(frac=1))
これは各パーティション内でのシャッフルです。パーティション間でデータが混ざりません。
真のパーミュテーションではありませんが、近似として機能します。完璧を求めるなら：
python# 全データを一度computeしてシャッフル（メモリ消費）
# または Dask の shuffle() を使用（複雑）
しかし、現実的にはこのままで十分です。
######################################################################################